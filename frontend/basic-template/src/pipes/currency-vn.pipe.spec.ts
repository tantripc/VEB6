import { CurrencyVNPipe } from './currency-vn.pipe';

describe('CurrencyVNPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyVNPipe();
    expect(pipe).toBeTruthy();
  });
});
